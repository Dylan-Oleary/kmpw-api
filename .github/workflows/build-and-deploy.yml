name: Build and Deploy Application

on:
  push:
    branches:
    - feat/upstash

jobs:
  build-api-image:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - id: 'auth'
      uses: 'google-github-actions/auth@v0'
      with:
        credentials_json: '${{ secrets.GCP_GITHUB_CLOUDBUILD_CREDENTIALS }}'
    - name: 'Set up Cloud SDK'
      uses: 'google-github-actions/setup-gcloud@v0'
    - name: 'Use gcloud CLI'
      run: 'gcloud info'
    - name: 'Start Google Cloud Build trigger'
      run: |
        export CLOUDSDK_CORE_DISABLE_PROMPTS=1
        gcloud beta builds triggers run ${{ secrets.GCP_BUILD_API_IMAGE_TRIGGER_ID }}
  # check-database:
  #   needs: build-api-image
  #   steps:
  #   - name: Test
  #     run: 'echo "Testing Build"'
